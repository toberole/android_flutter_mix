import 'package:flutter/material.dart';

class FlutterPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("FlutterPage"),
      ),
      body: Text("flutter page"),
    );
  }
}
