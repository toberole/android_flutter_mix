import 'package:flutter/material.dart';

class MyFlutterActivityPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: "MyFlutterActivityPage",
      home: Scaffold(
        appBar: AppBar(
          title: Text("MyFlutterActivityPage"),
        ),
        body: Text("MyFlutterActivityPage"),
      ),
    );
  }
}
