import 'package:flutter/material.dart';

class ThirdFlutterActivityPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: "ThirdFlutterActivityPage",
      home: Scaffold(
        appBar: AppBar(
          title: Text("ThirdFlutterActivityPage"),
        ),
        body: Text("ThirdFlutterActivityPage"),
      ),
    );
  }
}
