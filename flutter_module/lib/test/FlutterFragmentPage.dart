import 'package:flutter/material.dart';

class FlutterFragmentPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: "FlutterFragmentPage",
      home: Scaffold(
        appBar: AppBar(
          title: Text("FlutterFragmentPage"),
        ),
        body: Text("FlutterFragmentPage"),
      ),
    );
  }
}
